name: make-release

on: 
  push:
    tags:
    - '*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v2

    - run: |
        cd ${{ github.workspace }}
        mkdir zipped
        cd normalize-plex-files
        zip -r ../zipped/normalize-plex-files.zip *
        cd ../zipped
        (echo '#!/usr/bin/env python3'; cat normalize-plex-files.zip)> normalize-plex-files

    - name: test archiving
      uses: actions/upload-artifact@v3
      with:
        name: zipped-versions
        path: |
          zipped/normalize-plex-files.zip
          zipped/normalize-plex-files

#    - uses: ncipollo/release-action@v1
#      with:
#        artifacts: "release.tar.gz,foo/*.txt"
#        bodyFile: "body.md"
